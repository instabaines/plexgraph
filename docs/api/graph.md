# Graph model

The unified intermediate representation every loader builds and every view (plain, multiplex,
temporal, hypergraph) reads from. See [Architecture](../architecture/plan.md) for why it's one
structure instead of several.

::: plexgraph.Graph

::: plexgraph.Node

::: plexgraph.Connector

::: plexgraph.Layer
