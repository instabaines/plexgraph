# Tools that build the documentation site (mkdocs.yml). Not needed to use plexgraph itself --
# see requirements-dev.txt / `uv sync` for the development environment.
mkdocs>=1.6
mkdocs-material>=9.5
mkdocstrings[python]>=0.26
pymdown-extensions>=10
ruff>=0.6  # formats the signatures mkdocstrings renders; this repo already uses it (see [tool.ruff] in pyproject.toml)
