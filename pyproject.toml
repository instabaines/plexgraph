# Development environment for the whole monorepo. Not published: the installable packages are
# packages/core (hyperloom-core) and packages/bridge (hyperloom-bridge).
#
#   uv sync                                  # everything: both packages, optional loaders, tests, notebooks, benchmarks
#   pip install -r requirements-dev.txt      # the same, without uv
[project]
name = "hyperloom-workspace"
version = "0.0.0"
description = "Development environment for the hyperloom monorepo"
requires-python = ">=3.11"
dependencies = [
    "hyperloom-core[all]",
    "hyperloom-bridge[jupyter]",
]

[dependency-groups]
# uv installs the `dev` group by default.
dev = [
    "hyperloom-core[dev]",
    "hyperloom-bridge[dev]",   # pytest, pytest-asyncio, ipykernel, nbclient, nbformat, jupyter_client
    "scipy>=1.11",             # benchmarks/ and evaluation.ipynb
    "python-igraph>=0.11",
    "matplotlib>=3.8",
]

[tool.uv]
package = false

[tool.uv.sources]
hyperloom-core = { workspace = true }
hyperloom-bridge = { workspace = true }

[tool.uv.workspace]
members = ["packages/core", "packages/bridge", "packages/widget"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
