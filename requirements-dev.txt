# One Python environment for everything in this repo (both packages, tests, notebooks, benchmarks):
#   python -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt        # .venv\Scripts\pip on Windows
# With uv, `uv sync` does the same from pyproject.toml.
-e packages/core[all,dev]
-e packages/bridge[jupyter,dev]
-r benchmarks/requirements.txt
